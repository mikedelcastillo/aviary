# Copy this to models/immich/config/accounts.yaml and edit for your Immich users.
# Keep the server URL/IP in .env as IMMICH_BASE_URL.
accounts:
  - slug: account_1
    name: Account 1
    api_key_env: IMMICH_ACCOUNT_1_API_KEY
    album_name: Birds
    enabled: true

  - slug: account_2
    name: Account 2
    api_key_env: IMMICH_ACCOUNT_2_API_KEY
    album_name: Birds
    enabled: true
